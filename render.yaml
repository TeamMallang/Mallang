services:
  - type: web
    name: mallang-backend
    runtime: python
    rootDir: backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn solo:app --host 0.0.0.0 --port $PORT"